[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "3.0.0"
google-generativeai = "0.3.2"
python-dotenv = "1.0.0"

[dev-packages]
pytest = "*"

[requires]
python_version = "3.11"

[scripts]
start = "python app.py"
production = "FLASK_ENV=production python app.py"
test = "pytest"
